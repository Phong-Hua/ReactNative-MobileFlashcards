/**
 * Create a question Object from question and answer.
 * @param {*} question 
 * @param {*} answer 
 */
export function initQuestion(question, answer) {
    return {
        question,
        answer
    }
}